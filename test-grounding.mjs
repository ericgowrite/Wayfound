// Temporary diagnostic script — sandbox had no network access to run this,
// and the sandbox couldn't delete the file afterward either.
// Safe to delete manually; contains no secrets (reads key from .env.local at runtime, never wrote it here).
